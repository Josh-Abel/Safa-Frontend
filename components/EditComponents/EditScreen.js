import { View, Text } from 'react-native';

function EditScreen() {
    return <View style={{ flex:1, justifyContent: 'center', alignItems: 'center' }}><Text>To edit a word (including a conjugation), hold down on the word for 1 second!</Text></View>
};

export default EditScreen;